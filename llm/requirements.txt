# No third-party dependencies: the model server talks to LM Studio's
# OpenAI-compatible API over HTTP using only the Python standard library.
# Any python3 works — no venv required (see LLM_PYTHON in local.env).
